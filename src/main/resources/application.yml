server:
  port: 8085
logging:
  level:
    ROOT: INFO
spring:
  profiles:
    active: prod
  application:
    admin:
      enabled: TRUE
management:
  endpoints:
    web:
      exposure:
        include: info, health, beans

messageSourceBasename: "/messages/messages"
MessageSourceEncoding: "Windows-1251"
country: "EN"
language: "en"

